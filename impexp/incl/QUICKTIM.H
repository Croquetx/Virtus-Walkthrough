/*------------------------------------------------------------------------------ * VWINDOWS_ - the window object toolbox * * The QuickTime Interface * Copyright _ 1991 by Virtus Corporation * All Rights Reserved * Written by David A. Smith * Virtus Corporation * 117 Edinburgh So * Cary, North Carolina    27511 * (919) 467-9700 *------------------------------------------------------------------------------ * VQuickTime.h *------------------------------------------------------------------------------*/#ifndef VQUICKTIME#define VQUICKTIME#include "VFile.h"#if MACINTOSH#include <Movies.h>#include <QuickTimeComponents.h>	// is this necessary?  JCA (yup, it is)#endifstruct QuickTime:V_File{#if MACINTOSH	Movie qtMovie;		/* QuickTime movie reference 				*/	Track qtTrack;	Media qtMedia;			ImageSequence	seqID;/* Which sequence is being compressed */	Handle compressionBuffer;	ImageDescriptionHandle idescH;#endif //MACINTOSH	struct V_Offscreen *theOffscreen;	int speed;	long kVideoWidth;	long kVideoHeight;	Rect rect;		void cQuickTime(int q,long tp,long crt,strPtr filt,strPtr qstr);	void dQuickTime(void);	int OpenMovie(int createFlag);	int CloseMovie(void);		/*QuickTime I/O methods*/	int BeginMovie(Rect *rsize,int depth,int sp);	int EndMovie(int lastErr);	struct V_Offscreen *GetOffscreen(void);	int IORect(Rect *r);	int IOFrame(void);	};	#endif //VQUICKTIME