/*------------------------------------------------------------------------------ * WalkThroughª - the real time 3-D CAD system. * Version 0.1 * * Copyright © 1989 by Virtus Corporation * All Rights Reserved * Written by  	David A. Smith *				David W. Easter *				Mark J. Uland * Suite 204 * 117 Edinburgh South * Cary, North Carolina    27511 * (919) 467-9700 *------------------------------------------------------------------------------ * ColorPane.c *------------------------------------------------------------------------------ */#include "VTypes.h"#include "TEXTMENU.h"#include "PopColor.h"#include "CNTRL.h"#include "CVWCNTRL.h"#include "CadCNTRL.h"#include <string.h>#include "AppDeflt.h"#include "ToolDef.h"#include "Misc.h"#include "VGraphic.h"void TextureMenuSelect::SetController(struct CAVWalkController *ctrl)	{ 	controller = ctrl; 	}	void TextureMenuSelect::CalcSize(Rect *r,Point *p,int type,int warning)	{		r->left = r->right - 15;		r->bottom = r->top + 15;	}	void TextureMenuSelect::Display(int type,int x,int y)	{//JAM PORTTOWINQQQ#if MACINTOSH	Rect	r;	r = dataRect;		EraseRect(&r);			VForeColor(&vBlack);			FrameRect(&r);	InsetRect(&r,2,2);	PaintRect(&r);		VForeColor(&vBlack);	MoveTo(r.right-7,r.top+2);	LineTo(r.right-7,r.top+8);	MoveTo(r.right-6,r.top+3);	LineTo(r.right-6,r.top+7);	MoveTo(r.right-5,r.top+4);	LineTo(r.right-5,r.top+6);	MoveTo(r.right-4,r.top+5);	LineTo(r.right-4,r.top+5);	FrameRect(&r);#else	//it's windows//JAM PORTTOWINQQQ#endif //MACINTOSH	}		void TextureMenuSelect::MouseDown(Point p,long modifiers)	{	//JAM PORTTOWINQQQ#if MACINTOSH		delayTime = TickCount();#else	//it's windows//JAM PORTTOWINQQQ#endif //MACINTOSH	}	void TextureMenuSelect::Track(Point p,long modifiers,int lastCall)	{			}	void TextureMenuSelect::MouseUp(Point p,long modifiers)	{		}