#ifndef _TIMER_H_#define _TIMER_H_void InitTiming(void);			// call to compute the correction value for timer								// (not necessary unless you want the correction)void StartTimingMicro(void);		// call right before the code to be timedlong EndTimingMicro(void);			// returns time elapsed since StartTimingMicro() in microsecondsvoid StartTimingMilli(void);		// call right before the code to be timedlong EndTimingMilli(void);			// returns time elapsed since StartTimingMilli() in milliseconds#endif // _TIMER_H_