#ifndef FOCAL_PANE#define FOCAL_PANE#include "VPane.h"#include "VItem.h"#define CAMERA_LENGTH 130struct FocalPane:V_Pane	{	struct 	Controller 	*controller;	struct 	V_Offscreen	*slider;	struct 	V_Offscreen	*mask;	struct 	CameraText	*zoom;	int 		focal;		/*focal length in mm*/	int			fval;			int			lastRuler;	int			table[CAMERA_LENGTH+1];/*conversion table, put in a pixel, get out a focal length*/	int 		warn;		/*indicates we should use the short version of this */	int			tracking;	/*we are tracking the position of the lens*/	int			lastLoc;	/*last location of the mouse cursor*/	int 		offset;	Rect		rulerRect;	Rect		pictureFrame;		void 	SetFocal(int);	void	SetController(struct Controller *);		void	TrackHelp(Point,long modifier);// DAS-VWINDOWS 9/30/93	void    cPane(int);	void	dPane(void);	void	Display(int,int,int);		void 	DrawScene(void);	void	DrawSlider(void);	void	DrawBackGround(void);	void 	CalcSize(Rect *,Point *,int,int);		void 	MouseDown(Point,long);	void    Track(Point,long,int);			void 	MouseUp(Point,long);	int	 	KeyAction(char,long);		void 	TrackRuler(int,int);	void 	Update(void);	};struct CameraText:V_EditText{	int Action(char,long);	};#endif //FOCAL_PANE